import * as React from 'react'
import * as Blueprint from '@blueprintjs/core'


export default () => (
  <div style={{
    alignItems: 'center',
    height: '75%',
    justifyContent: 'center',
    display: 'flex',
  }}>
    <Blueprint.Spinner />
  </div>
)
